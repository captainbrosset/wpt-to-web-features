{
  "type": "module",
  "name": "wpt-to-web-features",
  "description": "Mapping of WPT test labels to web-features",
  "scripts": {
    "get-wpt": "git clone --depth 1 https://github.com/web-platform-tests/wpt.git --branch master --single-branch wpt",
    "delete-wpt": "rm -rf wpt"
  },
  "devDependencies": {
    "glob": "^11.0.3",
    "js-yaml": "^4.1.0"
  }
}
